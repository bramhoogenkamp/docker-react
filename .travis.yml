sudo: required
language: generic
services:
  - docker 

before-install:
  - docker build -t bramhoogenkamp/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bramhoogenkamp/docker-react npm run test